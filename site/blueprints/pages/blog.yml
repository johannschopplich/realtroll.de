title: Blog

icon: text

options:
  changeTemplate: false

columns:
  main:
    width: 2/3
    sections:
      articles:
        type: pages
        label: Artikel
        template: article
        sortBy: date desc
        info: "{{ page.date.toDate('d.m.Y') }}"

  sidebar:
    width: 1/3
    sections:
      meta: sections/meta
