{
  "private": true,
  "scripts": {
    "clean": "rm -rf public/assets/css",
    "uno": "unocss \"site/{layouts,snippets,templates}/**/*\" --out-file src/uno.css",
    "sass": "sass --style=compressed src/main.scss public/assets/css/main.css",
    "dev": "pnpm run clean && concurrently \"pnpm run uno -- --watch\" \"pnpm run sass -- --watch\"",
    "build": "pnpm run clean && pnpm run uno && pnpm run sass -- --no-source-map && hashup --ext css public/assets"
  },
  "dependencies": {
    "duecss": "^0.25.7"
  },
  "devDependencies": {
    "@unocss/cli": "^0.30.11",
    "concurrently": "^7.1.0",
    "hashup": "^0.3.2",
    "prettier": "^2.6.2",
    "sass": "^1.49.11",
    "unocss": "^0.30.11"
  }
}
